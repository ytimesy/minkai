# Agent Policy

このサイトを自律的に開発・改善する。
以後、このプロジェクトで作業するときは、この方針を優先して進めます。

最優先の成果は、訪問者が次をすぐ判断できる状態を作ることです。

- どんなアプリがあるか
- どの顧客に向いているか
- すでに触れる公開サイトがあるか
- どの導入形態で相談できるか
- 次に何をすればよいか

## 作業完了ルール

このプロジェクトでユーザーに見える修正を行ったら、原則として次まで自動で完了する。

- 変更内容を検証する
- コミットする
- `git push origin main` でプッシュする
- `./bin/deploy-site` で本番へデプロイする
- 公開URLの反映を確認する

ユーザーから明示的に止められた場合、または認証・権限・外部サービス障害で続行できない場合だけ停止して報告する。

## Googleタグ対応

新しいアプリ・ページ・導入導線を追加するときは、Google tag の対応を常に確認する。

- 既存の Google tag ID `AW-11111500959` を継続して使う
- 新しい公開ページでタグが読み込まれる状態にする
- 問い合わせ・資料請求・外部アプリ遷移など、成果として扱う導線では `gtag` イベントまたはコンバージョン送信を維持・追加する
- 日本語版と英語版の両方を変更する場合は、両方で計測が欠けないことを確認する
- 実装後は、タグの初期化・イベント送信が壊れていないことを検証項目に含める
